# Skills

One directory per skill: `skills/<name>/SKILL.md`, with optional supporting files (`references/`, `examples/`) alongside it.

Skills are namespaced by the plugin name and invoked as `/nagini:<name>`; description-based auto-triggering works as usual.
